# --- 编译构建产物 ---
# 你的 Makefile 将所有编译结果都放在 build 目录中
build/

# 如果偶尔会在根目录生成可执行文件（以防万一）
keyping
*.exe
*.out

# --- 运行时数据 ---
# SQLite 数据库文件（不应提交到代码库，这是用户的隐私数据）
*.db
keyping.db

# --- 编辑器/IDE 配置 ---
# VS Code 配置 (个人偏好设置不建议提交，除非团队强制统一)
.vscode/

# 其他常见的编辑器临时文件
.idea/
*.swp
*~
*.DS_Store

# --- C 语言通用忽略 ---
# 依赖文件 (如果它们出现在 build 目录外)
*.d
# 目标文件 (如果它们出现在 build 目录外)
*.o